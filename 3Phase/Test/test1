PROGRAM example;
	VAR x,y: INTEGER;
	FUNCTION gcd (z,b: INTEGER):INTEGER;
	  BEGIN
		IF a=0
		  THEN RETURN b
		  ELSE RETURN gcd(b,a MOD b)
	  END;
	  BEGIN
		READ(x);
		READ(y);
		WHILE(x<>0) OR (y<>0) DO
			BEGIN
				WRITE(gcd(x,y));
				READ(x);
				READ(y)
			END
	  END


