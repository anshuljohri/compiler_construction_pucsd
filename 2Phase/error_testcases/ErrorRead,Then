PROGRAM example;
 VAR x,y :INTEGER;
FUNCTION gcd (a,b:INTEGER):INTEGER;
 BEGIN
IF b=0 
 RETURN a
ELSE RETURN b
END;

BEGIN 
READ( );
READ( );
WHILE (x<>0)OR(y<>0)DO
 BEGIN 
WRITE (gcd(x,y));
READ (x);
READ(y)
END
END
